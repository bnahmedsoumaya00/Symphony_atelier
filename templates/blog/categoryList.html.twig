{% extends 'base.html.twig' %}

{% block title %}
Liste des Catégories – Soumaya
{% endblock %}

{% block body %}
<div class="container my-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="display-5 fw-bold text-purple"> Catégories</h1>
        <a href="{{ path('new_category') }}" class="btn btn-primary">
             Nouvelle Catégorie
        </a>
    </div>

    {% if categories is empty %}
        <div class="alert alert-info text-center">
            <h4>Aucune catégorie disponible</h4>
            <p>Créez votre première catégorie pour organiser vos articles.</p>
            <a href="{{ path('new_category') }}" class="btn btn-primary mt-3">
                Créer une catégorie
            </a>
        </div>
    {% else %}
        <div class="row">
            {% for category in categories %}
                <div class="col-md-6 col-lg-4 mb-4">
                    <div class="card h-100">
                        <div class="card-header">
                            <h3 class="card-title mb-0">{{ category.titre }}</h3>
                        </div>
                        <div class="card-body">
                            <p class="card-text">
                                {{ category.description|default('Aucune description') }}
                            </p>
                            <div class="text-muted small mb-3">
                                📝 {{ category.articles|length }} article(s)
                            </div>
                        </div>
                        <div class="card-footer bg-transparent border-top-0">
                            <div class="d-flex gap-2 justify-content-between">
                                <a href="{{ path('edit_category', {'id': category.id}) }}" 
                                   class="btn btn-outline-warning btn-sm">
                                     Modifier
                                </a>
                                <form method="post" 
                                      action="{{ path('delete_category', {'id': category.id}) }}" 
                                      onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette catégorie ?');"
                                      class="d-inline">
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.id) }}">
                                    <button type="submit" class="btn btn-outline-danger btn-sm">
                                         Supprimer
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% endif %}

    <div class="text-center mt-4">
        <a href="{{ path('article_list') }}" class="btn btn-secondary">
            ← Retour aux articles
        </a>
    </div>
</div>
{% endblock %}