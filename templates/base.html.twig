<!DOCTYPE html>
<html lang="fr" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}Mon Blog Symfony – Soumaya{% endblock %}</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        /* Modern Light Blue-Purple Theme */
        :root {
            --bg-primary: #e8f4f8;
            --bg-secondary: #d4e9f5;
            --bg-card: #ffffff;
            --border-color: #b8d4e8;
            --text-primary: #2c3e50;
            --text-secondary: #5a7a95;
            --primary-color: #7eb8dd;
            --primary-hover: #5da3ce;
            --secondary-color: #a8c5e4;
            --accent-color: #c4d9f0;
            --success-color: #6eb5d8;
            --danger-color: #b8a3d9;
            --purple-tint: #d4c5f0;
            --light-blue: #b3d9f2;
        }

        html, body {
            height: 100%;
            margin: 0;
        }

        body {
            background: linear-gradient(135deg, #e8f4f8 0%, #d4e3f5 50%, #e6dff7 100%);
            color: var(--text-primary);
            font-family: 'Inter', sans-serif;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        /* Modern Navbar */
        .navbar {
            background: rgba(255, 255, 255, 0.95) !important;
            backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(126, 184, 221, 0.3);
            box-shadow: 0 4px 30px rgba(126, 184, 221, 0.15);
            padding: 1rem 0;
        }

        .navbar-brand {
            font-weight: 700;
            font-size: 1.5rem;
            background: linear-gradient(135deg, #7eb8dd, #b8a3d9);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            letter-spacing: -0.5px;
        }

        .nav-link {
            color: var(--text-secondary) !important;
            font-weight: 500;
            transition: all 0.3s ease;
            padding: 0.5rem 1rem !important;
            border-radius: 10px;
            margin: 0 0.25rem;
            position: relative;
        }

        .nav-link:hover {
            color: var(--primary-color) !important;
            background: rgba(126, 184, 221, 0.15);
            transform: translateY(-2px);
        }

        .nav-link.active {
            color: var(--primary-color) !important;
            background: rgba(126, 184, 221, 0.2);
            box-shadow: 0 0 20px rgba(126, 184, 221, 0.3);
        }

        .nav-link i {
            margin-right: 0.5rem;
            font-size: 1.1rem;
        }

        .main-container {
            flex: 1;
            padding-top: 90px;
        }

        footer {
            background: rgba(255, 255, 255, 0.95);
            padding: 2rem 0;
            margin-top: 4rem;
            border-top: 1px solid rgba(126, 184, 221, 0.3);
            color: var(--text-secondary);
        }

        /* Flash Messages */
        .alert {
            border-radius: 15px;
            border: none;
            backdrop-filter: blur(10px);
            padding: 1.25rem 1.5rem;
            box-shadow: 0 4px 20px rgba(126, 184, 221, 0.2);
        }

        .alert-success {
            background: linear-gradient(135deg, rgba(126, 184, 221, 0.2), rgba(168, 197, 228, 0.2));
            color: #4a90b8;
            border-left: 4px solid #7eb8dd;
        }

        .alert-error {
            background: linear-gradient(135deg, rgba(184, 163, 217, 0.2), rgba(212, 197, 240, 0.2));
            color: #9178b8;
            border-left: 4px solid #b8a3d9;
        }

        .alert i {
            font-size: 1.25rem;
            vertical-align: middle;
        }

        /* Button Styles */
        .btn-primary {
            background: linear-gradient(135deg, #7eb8dd, #a8c5e4);
            border: none;
            color: white;
            box-shadow: 0 4px 15px rgba(126, 184, 221, 0.3);
            transition: all 0.3s ease;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            background: linear-gradient(135deg, #5da3ce, #8fb0d4);
            box-shadow: 0 6px 25px rgba(126, 184, 221, 0.4);
        }

        .btn-outline-primary {
            border-color: var(--primary-color);
            color: var(--primary-color);
        }

        .btn-outline-primary:hover {
            background: var(--primary-color);
            border-color: var(--primary-color);
            color: white;
        }

        /* Card Styles */
        .card {
            background: var(--bg-card);
            border: 1px solid rgba(126, 184, 221, 0.3);
            border-radius: 15px;
            box-shadow: 0 4px 20px rgba(126, 184, 221, 0.15);
        }

        .card:hover {
            border-color: rgba(126, 184, 221, 0.5);
            box-shadow: 0 6px 30px rgba(126, 184, 221, 0.25);
        }

        /* Badge Styles */
        .badge.bg-primary {
            background: linear-gradient(135deg, #7eb8dd, #b8a3d9) !important;
            box-shadow: 0 2px 10px rgba(126, 184, 221, 0.3);
        }

        /* Scrollbar */
        ::-webkit-scrollbar {
            width: 10px;
        }

        ::-webkit-scrollbar-track {
            background: var(--bg-primary);
        }

        ::-webkit-scrollbar-thumb {
            background: linear-gradient(180deg, #7eb8dd, #b8a3d9);
            border-radius: 5px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: var(--primary-hover);
        }
    </style>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-dark fixed-top">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_home') }}">Mon Blog</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('article_list') }}">
                        <i class="bi bi-journal-text"></i>Articles
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('new_article') }}">
                        <i class="bi bi-plus-circle"></i>Créer un article
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('category_list') }}">
                        <i class="bi bi-folder"></i>Catégories
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('new_category') }}">
                        <i class="bi bi-folder-plus"></i>Ajouter catégorie
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('article_par_cat') }}">
                        <i class="bi bi-funnel"></i>Recherche par catégorie
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('article_par_prix') }}">
                        <i class="bi bi-cash-coin"></i>Recherche par prix
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<div class="container main-container">
    {% for message in app.flashes('success') %}
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle-fill me-2"></i>{{ message }}
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    {% endfor %}
    {% for message in app.flashes('error') %}
        <div class="alert alert-error alert-dismissible fade show" role="alert">
            <i class="bi bi-exclamation-triangle-fill me-2"></i>{{ message }}
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    {% endfor %}

    {% block body %}{% endblock %}
</div>

<footer class="text-center">
    <div class="container">
        <p class="mb-0">© {{ "now"|date("Y") }} Mon Blog Symfony | Créé par Soumaya</p>
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>